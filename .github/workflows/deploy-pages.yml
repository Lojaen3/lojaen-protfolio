# Build Vite app and deploy to GitHub Pages.
# Repo → Settings → Pages → Build and deployment → Source: GitHub Actions

name: Deploy to GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm

      - run: npm ci
      - run: npm run build
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}

      # Same archive as actions/upload-pages-artifact, but upload-artifact@v6 is Node 24
      # (upload-pages-artifact@v4 still pins upload-artifact@v4.6.2 / Node 20).
      - name: Archive Pages artifact
        shell: sh
        run: |
          echo ::group::Archive artifact
          tar \
            --dereference --hard-dereference \
            --directory "$INPUT_PATH" \
            -cvf "$RUNNER_TEMP/artifact.tar" \
            --exclude=.git \
            --exclude=.github \
            --exclude=".[^/]*" \
            .
          echo ::endgroup::
        env:
          INPUT_PATH: dist

      - uses: actions/upload-artifact@v6
        with:
          name: github-pages
          path: ${{ runner.temp }}/artifact.tar
          retention-days: 1
          if-no-files-found: error

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        # @v4 is still node20 in action.yml; no newer tag yet. This SHA is main + node24 runtime.
        # Switch to actions/deploy-pages@v4 (or newer) after an official release bumps runs.using.
        uses: actions/deploy-pages@bc063e59bea150db1d7a4427d530a919fecaff34
